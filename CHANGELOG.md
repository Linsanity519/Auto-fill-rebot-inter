# 更新日志

每次发版，这一节会被送到两个地方（`tools/changelog.py` 负责抠）：

| 去处 | 内容 | 谁会看到 |
|---|---|---|
| `latest.json` 的 `notes` | **只有条目**，开头那句总结会被去掉 | 程序里「有新版本」那个弹窗 |
| GitHub Release 正文 | 总结 + 条目，原样 | 上 GitHub 翻的人 |

所以写的时候：

- **写"你会感觉到什么变化"，不要写提交标题**。"修了 `_grab` 的候选轮转"对用户
  没有意义，"抢不到会议室时不再空转十分钟"才有。
- **开头那句总结是给 Release 页面的**，弹窗里不会出现，所以别把只写在总结里的
  信息当成大家都看得到。真正要让人看到的，写成条目。
- 一节最多写五六条。弹窗那块版面很小，写满二十条等于没写。
- **可以只写一句总结、不写条目**（小修小补就这么写）。这种情况那句话会照原样进
  弹窗 —— `strip_lead()` 只在确实有条目时才去掉总结，不会把 notes 弄空。
- 只写发出去的东西。改 CI、改文档、改测试不用在这儿露面。

格式必须是 `## X.Y.Z` 开头，`tools/changelog.py` 按这个抠取；
CI 抠不到会**直接让发版失败**（见 `.github/workflows/release.yml`），
因为 notes 缺失是静默的 —— 发出去了才发现同事只看到一个版本号。

---

## 1.1.18

修「预定会议室」一间也抢不到 —— 有空房也抢不到，而且界面上骗你说「都被占了」。

- **修了抢会议室从头到尾一间也订不成**。会议后台 8 月之后给提交预定加了一道浏览器
  安全校验，我们的包里没有那几个字段，于是每一间都被拒。**这一条是所有人都中招的，
  不是偶发**。现在会向页面自己要这份校验再提交，实测已能正常订到。
- ⚠ **升上来之后要把 Chrome 整个关掉重开一次**（任务栏图标也退掉），再点
  「启动浏览器并登录」。那道校验需要浏览器用新参数启动，光升级程序不够。
  忘了也不要紧：一开跑就会拦住你并把这句话说一遍，不会闷头跑十分钟。
- **抢不到时，界面上会直接显示后台的原话**。之前后台明明回的是「浏览器安全校验
  未通过」，界面上却翻译成「15 间刚被占」—— 因为那句话里带「请刷新」，被程序
  当成了「刚被人抢走」。现在不再替后台编理由，它说什么就显示什么。
- **不再空转十分钟**。该试的会议室一轮试完就收摊并给结论，不会拿同样几间房反复
  撞同一堵墙撞到超时。
- **查空房快了一大截**（实测一轮从 3 分半降到几十秒）。改成让服务端按时段筛，
  并且拿够能用的就停止翻页 —— 之前是把全站三百多间整包拉回来自己筛，
  后台一变慢就直接卡死，连心跳都不打。

## 1.1.17

修两个都是「静默坏掉」的毛病：常规商广整批跑到一半失败，以及统计表一直没有数据。

- **修了常规商广跑到中间某一条突然报「没找到打开批量填标题抽屉的按钮」**。按钮一直
  都在，是程序找早了：刚传完 10 个视频，创意表单还在渲染，它只看了一眼没看到就报错。
  现在会等到它出现为止。这也是为什么前面几条都好好的、偏偏中间某一条失败 ——
  机器越慢越容易撞上，跟网络没关系。
- **顺手堵了一个更难发现的**：创意切换动画没走完时，有可能把标题、描述、落地页
  填到**上一条创意**上，页面不会报任何错。现在这种情况会直接停下来说清楚。
- **修了统计表一直收不到数据**。1.1.15 加的表格回传，地址只跟着 45MB 完整安装包走，
  而日常升级用的是 300KB 的小包 —— 于是绝大多数人的表格通道从来就没打开过，
  而且一声不吭（企微群那条一直是通的，所以看不出来）。现在地址跟着小包一起发。
- **升上来会把之前漏掉的历史补进表里**。之前那些只发进群、没进表的运行，
  这一版启动时会自动补一趟，不用手动做什么，群里也不会被历史刷屏。

## 1.1.16

收集端换成直接读那张统计表，不用再复制聊天记录了。

- **统计不再靠「去群里全选复制」**。`tools\collect_usage.py` 现在默认直接读企微智能表格
  （几条 SQL），出 `config/team.json`。要收还没升级的人发到群里的老消息，加 `--clipboard`。
- **首页「全团队」那张卡的数字口径没变**：拿新算法跑出来的结果和现有 team.json 逐项比过，
  人数 / 条数 / 失败 / 机器秒 / 省时 / 每周 / 分类型全部一致。
- 1.1.15 之前的老数据（每人每周的累计）单独放在表里的「历史周汇总」子表，
  和「一次运行一行」的新数据粒度不同，聚合时分开算、结果合并。
- 顺手删掉了「每周累计 + 上报记账」那一整套（400 行）：回传改成一次运行一条之后
  它就没人用了。口径现在只在收集端算一次，不会再出现「首页我的」和「首页团队」
  两个不一样的省时数字。
- **修了运行模式在统计里显示成英文的问题**。「逐条确认」是默认那一档、也是最常用的，
  但它在回传里一直写着 `confirm`；另外 `step` 本该是「逐步试跑」，之前标成了「逐条确认」。

## 1.1.15

统计回传多一条通道：直接写进企微智能表格，一次运行一行。

- **回传现在会往一张企微智能表格里写**（「配置助手 · 使用统计」的「运行流水」子表），
  一次运行一行：什么时候跑的、什么版本、哪个配置类型、什么模式、几条、机器花了多久。
  表本身就是个能打开就看的看板，不用等谁去整理。企微群那条照旧发着当兜底。
- **升级后第一次开程序，会把你这台机器上的历史运行一次性补进去**。以前的统计只有
  「每人每周的累计」，粒度对不上；而本机埋点本来就是一次运行一条，是真的明细 ——
  补上去之后，表里从第一天起就是全的。只补一次，重复开程序不会灌重复数据。
- **两条通道各记各的账**：表格写成功、群没发出去时，下次只补群那一条，
  表里不会多出重复行。

## 1.1.14

统计回传改成「一次运行一条」，不再发每周累计。

- **回传的内容现在就是「这一次运行是什么」**：什么时候跑的、什么版本、哪个配置类型、
  什么模式、总共几条、成/败/跳各几条、机器实跑多少秒、等人点确认多少秒。
  以前发的是「本机这一周到现在的累计」，想知道刚跑那轮干了什么只能拿两条消息相减，
  而每轮的类型/模式/耗时/时刻全被压掉了。累计是收集统计那头该算的事。
- **跑完先落盘再发**（`output/usage-outbox.jsonl`）。跑完那一下人常常直接叉掉窗口，
  后台线程跟着没了 —— 现在落了盘，下次开程序自动补发；发出去才划掉，失败就留着。
  攒了多条时打包成一条消息发，不会把群刷屏。
- 首页那句提示跟着改成「还有 N **次运行**的使用统计没回传成功」。

## 1.1.13

接着修「常规商广」的素材标题：弹窗打开了、6 条都填进去了，卡在最后点不到「确认」。

- **「『添加标题』抽屉里没有『保存』按钮」修了**。那个弹窗底下的按钮实测叫「**确认**」，
  代码里写死的是「保存」。现在「确认 / 保存 / 确定」是别名表，弹窗本身的类名
  （老的 `.batch-title-drawer` 已经不在页面上了）和「已选 6/6」那个计数也一样改成别名表。
  同一段里这三处后台各改过一次名，每次的表现都是整批第一条就失败。
- **计数读得到了，「某条标题被页面悄悄拒掉」才拦得住**。以前按「已添加」找，
  而页面写的是「已选」，读不出来就整段跳过校验 —— 违禁词被吞掉也没人知道。
- 新增 `tools\probe_ad_reg_titles.py`：**挂到已登录的调试 Chrome 上只读地核一遍**
  「页面现在到底把这几个按钮叫什么」。离线测试测的是「几种叫法都认得」，
  测不了「今天叫什么」—— 这三次翻车都是后者。

## 1.1.12

「常规商广」换成新版：视频数量决定规模、文案挪到准备页，外加十来处稳定性修复。

- ⚠ **1.1.11 里的「常规商广」是旧版**（吃 Excel、单元数看 Excel 行数）。新版一直没并进主干，1.1.11 从主干打包就把它带了回去 —— 这一版把新版并进来了，已经升到 1.1.11 的直接更新到这一版。

- **「视频数量」现在真的决定规模了**。以前建几个单元看的是 Excel 有几行，那个标着必填、注释写着「填 50 = 5 个单元」的框其实什么都不管 —— 填 150、Excel 里 6 行，结果只跑出 1 个单元。现在只有一个来源：视频数量 ÷ 10 = 单元数（150 → 15 个单元 × 10 条创意，一条创意一个视频）。
- **素材标题 / 素材描述 / 落地页挪到「准备」页填一次，全批共用**，这个配置类型不用 Excel 了。原来要 15 个单元就得手填 150 行、而这 150 行内容一模一样。「素材标题」是个多行框，一行一条、最多 6 条。
- **准备页填的东西，「载入并检查」之前会自动存**。以前必须先手动点一下那个「保存」按钮，不点的话后台读到的还是上一次存盘的值 —— 于是就出现了「计划名称我填了，日志偏说『[准备阶段]「计划名称」没填』」。第一次用某个配置类型时最容易撞上：存盘文件压根还没生成，后台看到的全是默认值。「抢占任务清单」（预定会议室）同样处理。
- **准备页填错了会拦住整批**，不再是「第 1 个单元被拦下、第 2~15 个照样拿着空标题往下跑」。
- **说明性的提醒不再把整批堵死**。像「最后一个单元不满 10 条」这种话，以前算「问题」，而有问题的行是不跑的 —— 只有一个单元时就等于「开始」按不动。现在核对表里显示成「✓ 可以跑 · 提醒：…」，照跑不误。
- 「生成 Excel 模板」按钮点了会告诉你常规商广不用模板，该去哪儿填。
- **翻页翻到一半报「超出我的视频总数」修了**。287 个视频、跑到第 6 个单元报「第 3 页只有 0 个」—— 其实是翻页之后只干等 1.6 秒就去数卡片，列表还没拉回来。现在等到「页码切过去了 **且** 卡片渲染出来了」才继续，上限跟设置里的超时走。
- **报错不再指错方向**。「这一页没渲染出来」和「视频真的不够用」现在是两句不同的话：真不够会告诉你要取到第几个、把「视频数量」或「跳过前几个」调多少；没渲染出来会直接说重跑一次通常就好。
- **同一天重跑不会再莫名跳过前面几个单元**。单元名是「常规商广_日期_序号」，同一天先试跑 20 个、再正式跑 150 个，正式那轮会把前两个单元当成「已经建过了」跳掉 —— 表现就是前 10 个视频没配。断点现在带上这一批的指纹（计划名 + 视频数量 + 跳过前几个），换了参数就是另一批。
- **Story 组件列表偶尔拉空导致整个单元失败，现在会自动重开重试**。第 8 个单元卡在「点了『选择』但 Story 组件抽屉没出来」，看截图抽屉其实是开着的，只是「共有 __ 个附加创意组件」那个数字是空的 —— 后台没返数据。现在这种情况会关掉重开、最多试 3 次；还是空才报错，而且会告诉你抽屉上究竟写的什么，以及「抽屉没打开」和「打开了但是空的」是两回事。
- **这套页面的操作不再靠「干等几秒」赌页面好了没**。整个常规商广的填写逻辑里原来有 22 处写死的等待（等 400 毫秒、等 1.5 秒、等 2.5 秒…），网快了白等、网慢了就出各种「找不到 / 数不对」。现在一律改成等一个具体的条件成立（下拉真的收起来了、「已添加 6 条」真的涨上去了、创意块真的渲染出来了），上限跟「设置」里的超时走。慢的时候更稳，快的时候更快。
- **「空间设置」那句提示不再刷屏**。「稿件UP主空间」对没绑 UP 主空间的视频是禁用的，页面自己会落到品牌头像那一档 —— 这是正常兜底，但以前每条创意都记一行，50 条创意刷 50 行，真正的报错被埋掉了。现在这一批里只在第一次记一条，并写清楚落到了哪个空间。

## 1.1.11

修「常规商广」点不到「添加标题」，整批第一条就失败。

- **「常规商广」填素材标题时报「创意块里没有『批量添加』按钮」修了**。页面上那个按钮
  现在叫「+ 添加标题」，而代码里写死的是早先抓取记录里的「批量添加」，还要求文字完全
  相等 —— 按钮带个「+」图标，一个都匹不上，于是第 1 个单元就失败、整批停在那儿。
  现在这几个叫法（添加标题 / 批量添加 / 批量添加标题）当别名处理，点开的本来就是
  同一个抽屉；匹配也从「完全相等」改成「包含」，页面加个图标不会再把它打死。
  （「+ 新增标题」是另一个按钮，不会被误点。）
- **抽屉的类名也不再写死一个**：`.batch-title-drawer` 找不到时，退到当前可见的那个
  带文本框的抽屉。以前后台换一次皮，这里报的是「抽屉里没有可填的文本框」，
  人只会以为是页面没加载完。

## 1.1.10

修了创意文案填串位、省时口径、统计回传和抢会议室四处。

- **收银台弹窗：上按钮文案被下按钮文案覆盖、下按钮空着的问题修了**。两个框的占位提示
  都是「最多6个字」，之前按占位提示找框，两个值就都写进了上面那个；下按钮空着卡在
  必填校验上，表现是「保存创意」点了没反应、页面也不报错。现在填完还会回读对一遍，
  填串位当场就报，不用等跑完一整批才发现。（「下按钮文案」也跟着改成必填。）
- **首页「省下工时」不再减掉机器实跑的时间**。机器跑的那两三个小时你人不在旁边守着，
  从省时里扣掉等于「脚本越慢越不值钱」，方向反了。现在省下的就是同样这些活人工要花的时间。
- **统计回传带上「这周跑过哪几个配置类型」**。以前只回传每个类型**成功**了多少条，
  一次全失败的类型在消息里直接消失 —— 看上去就像每次回传的都是前一天那份。
- **抢会议室：一次没抢到不再把那天永久标成「了结过」**（只有每周循环的任务才记）。
  以前单次任务抢不到就得点「清除断点」才能重抢，而那会连别的任务抢到的记录一起清掉。
- **抢会议室：不再把「有房只是被别人占着」误报成「全被服务端拒绝」**，也不会因此提前收摊；
  等窗口重开之后，10 分钟的抢占预算会重新计时，不再被等待那段吃掉。


## 1.1.9

修 1.1.8 的一个硬伤：「常规资源位批量开关」里的「本工具操作过的」点开一直转圈。

- **「本工具操作过的」不会再卡在「读取中…」了**。1.1.8 那版这一档点开就是一个永远转的圈，日志里还一个字都没有 —— 界面向后台要台账时多传了一个参数，后台那个方法收不下，这类错在中间那层是**静默**的：请求既不成功也不失败，界面就一直等。顺带把这条路补严了：以后但凡界面调后台出这种错，会直接在运行日志里写明是哪个调用挂了，而不是让你对着转圈猜。
- **勾选那几批现在真的管用**：列出来的是「这次方向能翻回的那几批」（方向=开启就只列上次被本工具暂停的），勾中会高亮、下面实时显示「已选几批共几条」，点「载入并检查」只翻你勾的那几批。
- **一批都没勾时不会偷偷把所有批次都翻一遍**了：会拦下来提示你先勾。

## 1.1.8

新增配置类型「常规资源位批量开关」：把一个活动下的单元 / 创意批量启动或暂停投放。

- **新增「常规资源位批量开关」**（侧栏「大会员资源位」组）。填一个活动ID，选层级（单元 / 创意）和方向（启动 / 暂停），再按名称关键词或 ID 清单挑行，就能把投放列表里那一批一次性开或关。以前只能在后台一行一行点「更多 → 暂停投放」，一个活动几十上百个单元，点到手酸还容易漏。
- **活动ID 可以留空**：把「选哪些行」切到「按清单」、贴一串创意ID（或单元ID），工具就一个 ID 查一次列表、查到就点，不用先知道它们属于哪个活动。查不到的 ID 会在预检里单列一行写明，不会悄悄少几条。
- **新增「本工具操作过的」这一档**：跑完一批会记账（层级 + 活动 + 每条的ID），下次把范围切到它就能看到最近那几批、**逐批勾选**（选中会高亮、下面实时显示「已选几批共几条」），点「载入并检查」只翻你勾的那几批 —— 方向反过来就能把**刚暂停的那批原样恢复**（或者把刚开起来的那批再关掉），不用自己留 ID 清单。页面上按状态筛出来的是所有人暂停的，分不出哪些是你动的。
- **修了一个所有配置类型都有的界面问题**：窗口窄一点（约 980px 以下）时，「执行」页最右边的「开始配置」按钮会被裁到窗口外面，看着就是按钮没了；「跳过已成功的」也会被挤成竖排一列字。现在一行放不下会自动换行。
- **已经是目标状态的、后台压根没给开关的，自动跳过**。「未开始 / 已完成 / 已终止」这三种状态在后台的菜单里就没有启动和暂停两项，工具会在预检里直接标出来并说清原因，而不是跑到一半报一句看不懂的错。
- **「终止投放」永远不会被点到**。它就挨在开关旁边、点完不可逆，所以工具只认「启动投放 / 暂停投放」两项，点之前还会把二次确认弹窗的文字读一遍核对，对不上就取消。
- 每条的结果（层级、活动ID、ID、名称、成功/跳过/失败、原因）都会写进结果 csv，跑完能直接翻。
- **顺带带上 1.1.7 那批弹窗修复**（那一版没发出去）：更新说明、一长串报错这类长内容不会再把「立即更新 / 关闭」推到看不见的地方，按钮钉在底部、滚动条也看得见了；点弹窗外侧空白或按 Esc 也能关。

## 1.1.7

弹窗不会再"关不掉"了：按钮钉在底部，滚动条看得见。

- **弹窗里的按钮不再跟着内容一起滚**。更新说明、一行攒了十几条的报错这种长内容，以前会把「立即更新 / 以后再说 / 关闭」推到滚动区一千多像素以下 —— 屏幕上只剩一大片文字，看着就是弹窗关不掉。现在标题钉在上面、按钮钉在下面，中间那段自己滚，内容再长也点得到。
- **多了一条看得见的滚动条**。以前的滚动条只在滚的时候才现形，「这块还能往下滚」完全没有提示，容易以为内容就这么多、或者以为界面卡住了。
- **点弹窗外侧空白处、或者按 Esc，也能关掉弹窗**。跑批时那两个「提交这条 / 跳过 / 停止」的弹窗除外 —— 它们在等你的答案，随手关掉后台会一直挂着。

## 1.1.6

资源位投放：新增两个资源位、Excel 里的图现在是"传上去了才算数"；外加首页大改：统计口径统一成全团队，「数据统计」并回首页。

- **新增「追番tab小黄条」「影视tab小黄条」两个资源位**。选上就能生成模板、跑。创意层四项：标题文案(10字)、副标题文案(20字) 必填，按钮文案、跳转链接留空走页面默认（续费 / 购买页）。
- **Excel 里的图不会再"以为传上去了其实没传"**。以前判断传成功只看有没有一个网络请求回来，而图片预览的请求长得跟上传请求一样，于是偶尔提前放行 —— 一个单元 16 条创意里有两条的图是空的，页面不标红、点保存静默失败，日志里一点线索都没有。现在必须页面上真的出现预览图或回填了网址才算数，没传上去自动重传一次，再不行明确报错停下。
- **一个单元里多条创意时，不会再把后一条的内容填进前一条**；「会员中心banner」的两张图（1200*240 / 增大版 560*168）也不会再报「有 2 个上传框，不敢乱传」。这两条以前的表现都是"填到一半卡十几秒然后保存失败"。
- **两类以前读不出来的图现在能用了**：一是 WPS 的「嵌入单元格」图（格子里显示 `=DISPIMG(...)`），以前整批报「图片找不到」，现在会从文件里抽出来当素材传，真抽不出的那几张报错会直说「请改成浮动图片或填链接」；二是 `//i0.hdslb.com/...` 这种没带 `http` 的链接，以前被当成本地路径。
- **几处必填项按页面实际改对了**：「会员中心弹窗」补上漏配的「生效渠道」（必填、页面默认不选，缺它就卡在"点了保存并下一步但没跳转"，现在自动填「全局」）；PCbanner（PC顶导浮层）的 icon文案 / icon / Banner跳转链接 页面上本来就没红星，改回选填，不再被拦着跑不了。
- **「载入并检查」的详情弹窗不会再撑出屏幕、关不掉了**。一行攒了十几条报错时，弹窗现在内部滚动，「关闭」永远点得到；另外点弹窗外侧或按 Esc 也能关。
- **「数据统计」并回了首页，侧栏少一项**。原来那两页后面是同一批卡 —— 首页画一遍战绩，点「查看详情 →」进去第一张还是同一张。现在首页一页到底：战绩 → 这些数字怎么来的 → 用在哪儿了 → 谁在用 → 你自己最近跑的 → 还没试过的。顺带首页那张目录卡只列「还没试过的」了：用过的侧栏里天天见，在首页再铺一遍等于一个工具挂两份导航，十来个类型全铺开还占掉大半屏；全试过了这张卡自己消失。
- **首页那几张卡不再一半团队一半本机了**。以前顶上的「累计处理 / 一次做对 / 在用人数」是全团队的，可「本周 +N」「最近跑的」「最长的一次」却只算你这台机器 —— 一张卡上两套口径、还不标出来。结果是群里明明回传上来了，「最近跑的」里却一条都不多，看着像回传坏了。现在同一屏上的数全是团队的。
- **多了一张「谁在用」**：谁、累计跑了多少条、最后一次是什么时候。它替掉了团队口径下的「最近跑的」—— 回传上来的是每人每周的累计，本来就没有「第几次运行、跑了多久」这种粒度，硬凑一份只能是假的。你自己那份逐次流水还在，改名叫「你自己最近跑的」，标题里写明是谁的数。
- **另外两处数字不再瞎写**：「在用人数」下面不会再出现「另有 -1 人只打开过」；成败环形图也不再画一个永远是 0 的「跳过」（回传里根本不带这一项）。

## 1.1.5

新增配置类型「常规商广」（在「商业化广告」组，原生商广新/老 下面一项）。

- 和原生商广老同一个后台页面（`#/promote/auto`），但**推广目的 / 推广内容 / 转化目标**做成了三级联动下拉：选了推广目的，推广内容和转化目标的选项跟着变。目前保留「交易经营→电商链接」「内容种草→内容投放」「销售线索收集→线索」三组。
- 视频走「我的视频」：准备页填「视频数量 N」和「跳过前几个 K」，程序从你的视频列表跳过前 K 个、往下顺延取 N 个；**每 10 个视频归一个单元**（填 50 = 5 个单元），一个视频一条创意。
- 每条创意的**素材标题（一格里换行写多条，≤6）、素材描述、落地页**在 Excel 里逐行填 —— 生成模板、填好、载入并检查、跑。Story 转化组件和空间设置默认取第一个。
- 顺带：**准备页现在能上下滚了**。字段多的配置类型（常规商广、原生商广）以前下半截字段会被切掉、点不到。

⚠ 第一次跑常规商广时，建议先「空跑」一遍看截图确认，再逐条确认提交。

内部维护版：使用统计换了一个回传群。你**不用做任何操作** —— 无论是覆盖安装新包，还是在程序里点更新升上来，都会自动切到新群。

## 1.1.3

界面调了一版：更清爽，层级更清楚。

- 颜色分了工：**粉色只留给「这一屏该点的那个按钮」和选中态**，次一级的操作（载入并检查、体检、分享…）、导航、tab、链接统一走**蓝色**（B 站另一个主色）。以前满屏都在发粉光，分不出主次。
- hover 从粉色改成中性浅灰 —— 鼠标划过一行不再整片变粉。
- 字号收敛成固定几档、间距对齐到 4/8 栅格，少了很多 1px、2px 的错位抖动。
- 键盘操作（比如逐条确认时按 Tab / 回车）现在有清楚的蓝色焦点框。
- 报错、ID、路径这些现在可以选中复制了（以前整个界面禁选）。
- 深色模式单独调了一版配色，不再是浅色直接反过来。

## 1.1.2

后台改版能提前发现；连不上浏览器说得清原因；Excel 缺列不再静默漏填；跑完能拿到产物清单、跑错能翻回。

- **体检当前页**：「准备」页新按钮，把 yaml 里记的字段 label / 按钮 / 弹窗判据拿到你当前打开的页面上挨个点一遍名，找不到当场标红 —— 后台一改版先点它，别等批量跑到一半才发现。（命中 ≠ 一定填得进，仍以实跑为准。）
- **连不上浏览器说得清**：不再是一句「浏览器没连上」，会分清没装 Chrome / 没带调试端口 / 停在登录页，每种给一句能照着做的下一步。
- **Excel 缺列不再静默**：选完数据文件自动把表头和「模板本该有哪几列」对一遍，缺列红字列出（以前缺列被直接忽略，跑到页面上才发现字段全空）。命令行也打同样一句。
- **抽样确认**：运行档多一档「抽样确认（前 N 条）」——前 N 条逐条确认，口径对了剩下的自动跑完。N 默认 3（`confirm_sample_n` 改，命令行 `--sample N`）。不用再手点 40 次，也不用一上来就赌全自动。
- **产物清单 + 一键翻回**：每批跑完 `output/manifests/` 下落一份逐条记录，执行页有按钮直接打开；「价格策略批量开关」还多出「把这批翻回去」，按清单反方向再点一遍（仍逐条确认）。
- **策略 / 准备页配置可分享**：「准备」页多「分享给同事 / 拉取同事的…」，走 GitHub 分支（不碰主干）传参数，拉取前自动备份。没配 token 时降级发企微通知。
- 老的 `--tk` tkinter 界面下线（配不了策略中心 /「准备」页 / 活动选择，早不是主力）。默认界面和 `--cli` 不受影响。

## 1.1.0

没有想要的助手？自己录一个 —— 通用化方案上线。

- 侧栏底下多了「＋ 自己录一个助手」。填个名字和要操作的网址，去浏览器里正常操作一遍，工具把你的点击 / 输入拼成一套可编辑的步骤；**在准备页点「本地试跑（逐步）」跑通一遍**，才收进《自制配置类型》并把这份工作流发去审核（企微群，或配了 token 就推 GitHub `selfmade/<名>` 分支，只碰分支不碰主干）。没跑通之前「核对」「执行」两步是锁上的。
- 录到的每步能看见它会用哪个选择器（文字 / label / 属性 / css），只有 css 兜底的会标红提醒。步骤能上下挪、能在任意位置插一步（停一下核对 / 等文字 / 校验 / 截图 / 按键）、能把「点开下拉 + 点选项」两步合并成一个「选下拉」、能给某一步切换「提交动作」标记；fill / select 的值就地改成 `{{列名}}` 绑 Excel，勾「按 Excel 每行跑一遍」就是批量。
- 「本地试跑（逐步）」会一步一步走：每步在页面上高亮当前要操作的元素、停下等你点「下一步 / 跳过这步 / 直接跑完 / 中断」，只填不提交。
- 录制结束时会抓一份整张表单的状态。回放跑完所有步骤后，会逐字段比对、把没对上的补上 —— 补的是「录制时你没点、但目标单元该有」的字段（比如新建单元时某个下拉的默认值和目标不一样）。之前脚本复刻出来的单元会在这类字段上和手工配的不一致，现在会自动对齐。准备页可以关掉「回放后对齐整表」只按步骤跑；按 Excel 每行跑的批量流程不做对齐（每行的值本来就不同）。
- 自制配置类型和正式的分开放、带「待审核 / 已采纳」角标；能不能转正、可靠不可靠，由维护者看送审的工作流 + 跑通日志决定。
- 侧栏 footer 顺手收拾了：五样塌成两行（版本号 / 检查更新·反馈·深色），没新版本不占地方；「打开结果目录」「清除断点」挪到「执行」页。
- 新增「反馈」入口：报告问题（可附最近运行日志）/ 提功能建议，发到企微群。

## 1.0.22

修掉「加了新配置类型之后，升级当天统计群里又刷出一堆早就发过的旧周」。

- 1.0.20 修过一次（版本号不该进「这一周变了没有」的签名），这次是同一个坑的另一半：**每个配置类型也占签名里一列**，加一个新类型（1.0.21 的「价格策略批量开关」）就让所有历史周的这一段整体移位、签名全失配，几十周旧数据被原样重发一遍。签名现在只认真正会变的数（次数/成功/失败/机器秒/最后活跃），配置类型明细不进签名——总数在里面就够了。
- 老的上报记账（1.0.19 及更早、以及 1.0.20/1.0.21 写的）读进来就地迁到新格式，升级当天不会因为「换了签名口径」这个动作本身再刷一遍。

## 1.0.21

新增配置类型「价格策略批量开关」——把策略中心「价格配置」表里那些配好却还停在「未开启」的行，一次全开；也能反过来一次全关。

- 「价格」下多了「价格策略批量开关」一项，界面上一个开关切「开启 / 关闭」方向。
- 三种范围：按名称关键词（子串命中人群名称）、本工具配置过的（「价格策略配置」跑过的那几批，按策略筛，可再填个日期只算那之后的）、整页全部。
- 已经是目标状态的行、以及「人群选组＝不限」的行（开启方向）自动跳过，核对页会写明原因。
- 「本工具配置过的」这一档能直接在卡里看到台账：「价格策略配置」跑过哪几批、配了哪些人群、在哪条策略下，还能按日期和策略筛。
- 主用法是自己在浏览器里打开要操作的那条策略页，选「当前打开的策略页」。跨策略（在「策略」框里填 编辑页URL / 路由ID / 业务ID）是尽力而为——这套后台对直接跳转策略页支持不稳，跳过去表加载不出来时会明确提示你手动打开。
- 「价格策略配置」每跑完一轮，会把这批配好的人群名称 + 所在策略记进台账，供上面「本工具配置过的」调用。

## 1.0.20

「数据统计」页换了个长相；每次更新完，统计群里不会再刷出一堆早就发过的旧记录。

- 修掉「每次升级，统计群里都刷出几周前就发过的旧记录」。原因是版本号被算进了
  「这一周的数变了没有」的判据里 —— 升一次级，所有历史周同时被判成「变了」。
- 回传的那条消息更干净了：不再带「周」和「花名」。「周」本来就能从「最后活跃」
  算出来，「花名」一直是空的。
- 「数据统计」页去掉了近 12 周那张柱状图 —— 十二个格子里常年只有两三格有数，
  读不出趋势。
- 「数据统计」页新增「这些数字怎么来的」：一个环形图看条数落在成功/失败/跳过哪一边，
  一组对比条看人工要花多久、机器实际跑了多久，差额就是省下的工时。
- 「用在哪儿了」每个配置类型现在是两条 —— 上面条数、下面省下的时间，
  一眼能看出跑得最多的那个未必最省时间。
- 「最近跑的」每行多了一条完成度。

## 1.0.19

「资源位投放」「价格面板配置」「原生商广」中途失败之后重跑，不会再把已经建好的
单元重建一遍了。

- 这三个配置类型以前**没有断点**：40 个单元跑到第 30 个失败，重跑会从第 1 个开始，
  前面 29 个在后台又建了一遍。现在跑成功的会记下来，重跑时勾上「跳过已成功的」就跳过。
- 侧栏的「清除断点」对这三个以前会报错，现在能正常清。想完整重跑一遍，
  点一次「清除断点」再跑就行。
- 用 `--tk` 打开老界面时会明确提示：那个界面配不了策略中心 /「准备」页参数 /
  活动选择，跑的是你上次在默认界面里配好、存下来的那一份。

## 1.0.18

「价格面板配置」的 PID 映射表现在自带一份，装完就能用。

- 点「生成模板」时，`data\价格面板配置_PID映射表.xlsx` 会直接铺上运营抄好的
  那份真表（5 套方案、190 多行 pid），不用再一格一格抄一遍。
- 已经有这个文件就**不覆盖** —— 自己维护的那份不会被冲掉。要换回自带的，
  把它删掉再点一次「生成模板」。

## 1.0.17

「价格面板配置」支持定向投放了：生效渠道选「定向」，整条链路照跑。

- 「投放配置」页上新增一项**生效渠道**（全局 / 定向）。选定向之后页面上换成
  另一套字段，Excel 模板也跟着变 —— 人群、生效平台、频次、内容设置、赠单片
  这些定向没有的列不会再出，省得填了没处填。
- 定向下**每个套餐都要填 pid**（页面少一个就默默不让保存），机器人自己填。
  填哪几个看**策略中心的「生效平台」**：只投 iPhone 就只挂 iPhone 的 pid，
  不再把安卓、PC、HD 的一起挂上去。策略中心那一栏留空才是「有几个填几个」。
- 策略中心里「生效平台」「运营商」「投放区域」「收银台类型」这几项以前只能选
  一个 —— 页面上明明是复选框，界面上却缩成了单选，现在能多选了。
- 保存被后台拒绝时会把后台那句话原样报出来（比如「加购商品和买赠pid不相同」）。
  以前只说「点了保存但页面没跳走」，对着一屏看着正常的表单根本无从查起。
- 填 pid 快了很多：改成把候选一次读全再逐个点，不再一个一个打字搜
  （一个单元十来个套餐 × 好几个 pid，原先光搜就要好几分钟）。
- 创意的图传上去没出预览会自动重传，三次都没出才报错 —— 以前偶发地静默留空。

## 1.0.16

新增「价格面板配置」：批量配收银台价格面板的单元和创意，在侧栏「价格」下面。

- 一整条链路都能跑：建活动（或挂到已有活动）→ 建单元 → 建创意，中间不用接手。
- 一行 Excel = 一个单元：名字、投放时间、投给谁（人群）、选中类型，以及每个套餐的
  创意文案。**创意列只出这次真用得到的** —— 上了面板的 SKU 才有列，配了买赠的
  才多出买赠那几列，不会甩给你 26 个套餐的空列。
- 「放哪几个套餐、怎么排、哪个搭售」都在**策略中心**里配一次、整批套用：套餐按顺序
  点选，点的顺序就是页面上从左到右；每张套餐卡片右边那个小角标点开就能选搭售
  （无 / 买赠 / 0元购 / 买赠+0元购）。面板个数不用管，面板2 空着就是一个面板。
- 每个套餐配什么 pid、买赠什么商品，在一份**单独的 PID 映射表 Excel** 里维护；
  一个平台一行，机器人按这个单元的「生效平台」把对应几个 pid 都填进去。
- 「套餐排列」那一排卡片机器人会自己拖成指定顺序，面板分界也会摆好。
- 创意里的图片三种填法都行：本地路径、图片网址，或者**直接把图贴进 Excel 那一格**。
- 填完照例停下来等你核对再保存 —— 套餐排列是拖出来的，值得扫一眼。
## 1.0.15

资源位投放填创意快了十几倍：一条创意从十来秒变成 0.7 秒。

- 填创意时每条都要白等 5 秒的两个死等去掉了（等一个永远等不到的页签状态、
  等一个永远不会变的控件数）。
- 跑的时候会把配置页拨到最前：被切到后台的标签会被 Chrome 降频，
  同一条创意前台 0.7 秒、后台 10 秒。
- 素材图改成开跑前一次性并发下好，不再填到图片那一列才卡住；
  网址失效的图会在开跑时就报出来，不用等跑到那一行才发现。

## 1.0.14

把「有新版本」的提示精简掉重复的部分。

- 弹窗里不再把改动说明重复两遍（开头那句总结去掉了，只留条目）。
- 弹窗里去掉了「更新时程序会自动关闭并重新打开」那句每次都念一遍的套话。
- 侧栏那行只报版本号和下载大小，不再把整段说明糊成一长条。

## 1.0.13

更新提示里现在会写清楚每个版本改了什么，不再只有一个版本号。

- 「发现新版本」的弹窗和侧栏，会显示这一版实际做了哪些改动。
- 历史版本的改动整理进了 `CHANGELOG.md`。

## 1.0.12

修了预定会议室：以前抢不到会一直抢同样几间，而且十分钟不吭声。

- 抢占时会**轮着试所有符合条件的会议室**，不再反复对同样几间发请求。
  以前一间房 40 秒能被打 7 次（按十分钟超时算约 105 次），现在最多 2 次。
- 目标日已经能订、却一间也抢不到时，**几十秒就结束并说清原因** ——
  是"这个时段一间空的都没有"还是"有空房但后台拒绝了"，而不是空等十分钟
  再给一句"超时没抢到"。掐 10 点抢的场景不受影响，照样一直守着。
- 抢的过程里每 30 秒报一次进度，不再像卡死了一样一声不吭。
- 接口出错导致整轮没跑起来时，结果会写明真实原因，不再报成"还没轮到"。
- 修了登录信息偶发读不到就整轮中断的问题，现在会自己重试一次。
- 修了标签页越开越多：登录态过期重新登录后，程序认不出已经开着的页面，
  每跑一次就新开一个。

## 1.0.11

修了更新完之后还一直提示「更新到你已经在用的版本」。

## 1.0.10

- 首页的团队使用统计不用等发版了，收集端更新完大家下次打开就能看到。
- 有新版本时会弹一次提醒 —— 以前只有侧栏一行小字，基本没人注意到。
  同一个版本只弹一次，不会每次开程序都糊你一脸。

## 1.0.9

日常更新从 98MB 降到 300KB。

- 拆成「代码包」和「完整安装包」两种：平时只下 300KB 的代码包（约 8 秒），
  只有运行时依赖变了才需要下 45MB 的安装包。
- 修了跨版本更新时表单定义没跟着更新，出现「新代码配旧表单」的问题。
- 修了统计上报在部分机器上静默失效。

## 1.0.8

修了下载更新时因为文件名含中文而失败。

## 1.0.7

首个带自动更新功能的版本。
