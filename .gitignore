# 登录凭据 —— 绝对不能分发或提交
.chrome-profile/
.chrome-profile-*/

# 运行产物
output/
build/
dist/
build.prepackage-*/
dist.prepackage-*/
*.spec
# 但主程序的打包配方要进仓库 —— 它不是 PyInstaller 生成的，是手写的
!build_app.spec

# 数据（可能含业务敏感信息）
data/
*.xlsx
*.xlsm
*.csv
# 价格面板的 PID 映射表**故意提交进仓库**（2026-08-26 决定）。
# 它是运营一格一格抄出来的 190 多行真 pid，新装的人开箱就得有 —— 不进仓库
# 就不进代码包，`生成模板` 只能铺一张空表，等于每个人都要再抄一遍。
# 取舍想清楚了：这些 pid 是价格面板的配置项，泄露的最坏情况有限，
# 而「人人手抄一遍」是必然会抄错的。要换一份就直接改这个文件、重新发版。
!assets/价格面板配置_PID映射表.xlsx

__pycache__/
*.pyc

# 企微文档机器人的 apikey —— 全权限，绝不能进仓库或分发包
tools/.mcp_key

# 统计回传地址：**故意提交进仓库**（2026-08-21 决定）。
# 取舍是明确做过的：这个 key 泄露的最坏情况只是有人往那个统计群里发消息，
# 而「统计静默失效」是查不出来的 —— 没人会发现自己那份数据没回传。
# 所以宁可让 key 可见，也要保证任何人、任何环境打出来的包统计都是通的：
# 本机打包、CI 打包、别人 clone 下来打包，都不需要先配一次 Secret。
# 真被滥用就换一把群机器人 key（config/webhook.txt 改一行、重新发版即可）。

# 打包时从 config/settings.yaml 拷过去的默认值副本，见 src/settings.py
assets/settings.default.yaml
