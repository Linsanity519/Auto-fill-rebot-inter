# 资源位投放 — 创意层（第三步）配置项

抓取方式：在测试活动「子凡测试25年」(activityId=708) 下，为每个资源位新建单元并保存进入创意页，逐个展开选项抓联动字段。**未做任何提交审核操作。**

## 重要发现

### 1. 创意层是两套系统

| 系统 | URL | 举例资源位 |
|---|---|---|
| 新版 | `rich-vip.bilibili.co/delivery/creativity?unitId=&position=` | 会员中心领福利资源位 |
| 老版 | `manager.bilibili.co/v3/#/vip/resource-delivery/originality/detail?unitId=&position=` | 播放页催费条、开通提示条 |

保存单元后会自动重定向到对应系统。

### 2. `position` URL 参数只是显示用

创意表单实际按 **单元绑定的资源位** 渲染，改 URL 的 `position` 不会切换表单 —— 所以每个资源位必须真的建一个单元。

### 3. 单元层「投放展示位置」在编辑态锁死

只有 `state=create` / `state=copy` 能选资源位。

### 4. 建单元的可靠路径：`state=copy`

`/delivery/unit/0?state=copy&copyId=<已有单元id>&position=<n>&activityId=708&activityName=...&activityState=1&activityType=5`

复制态会把投放时间等预填好。（直接 `state=create` 时 antd RangePicker 无法通过脚本填写。）

保存前需补的必填项（切换资源位后会被重置，**需要补两遍**）：
- 生效平台：至少勾 1 个
- 内容类型 / ep付费状态：若该资源位有，至少勾 1 个
- 内容黑名单等 radio 组：若无默认选中，选第一个
- 版本限制等 select：若显示"请选择"，选第一项

---

## 已抓取资源位

### 会员中心领福利资源位 —— 新版系统

```
上传创意素材
创意1
投放类型: 装扮 / 收藏卡 / 固定福利券 / 外部单通福利 / 通用
id
标题文案
副标题类型: 免费体验卡 / 大会员折扣
副标题模板
按钮文案
卡片色值
预览效果图
[新增创意]
```

### 播放页催费条 —— 老版系统

```
上传创意素材
创意1 / +添加创意 / 创意泛化
配置类型: 仅文案 / icon + 文案 / 图片 / 文案
添加定制字段
跳转类型: 活动页 / 收银台 / 跳转链接
拉起方式: 新页面打开 / 半屏打开
  （提示：APP 6.50 之前版本不支持半屏容器，链接跳转默认新页面打开）
```

### 开通提示条 —— 老版系统

**配置类型联动：**

| 配置类型 | 字段 |
|---|---|
| 仅文案 | 标题文案、副标题文案、按钮文案、角标文案、**固化权益（3个）**[选择固化权益/清空]、**底部图片(1125*288)**[上传] |
| 开卡赠礼 | 标题文案、**开卡赠礼(1~3个)**[选择开卡赠礼/清空]、按钮文案、角标文案 |
| 倒计时 | 标题文案、按钮文案、角标文案、**底部图片(1125*288)**[上传]<br>（注：倒计时开始/结束时间与单元时间一致） |

**跳转类型联动：**

| 跳转类型 | 额外字段 |
|---|---|
| 活动页 | — |
| 收银台 | **PC端跳转链接** |
| 跳转链接 | **PC端跳转链接** |

**拉起方式**：新页面打开 / 半屏打开

### 追番tab小黄条（pos 5）/ 影视tab小黄条（pos 6）—— 老版 v1

2026-09-04 在活动 708 建单元 41136 实抓。**两个位的表单一模一样**，只差 position_id。

单元层：通用字段 + **点击关闭冷却时间**（单位「时」，选填）。
⚠ 这两个位的「生效平台」只有 Android / iPhone / ipad / 鸿蒙phone / 鸿蒙pc / 鸿蒙tablet
六个，**没有 PC / iPad HD / Android HD / public** —— 策略中心配了也白配，
filler 会自动跳过页面上没有的项，不用为它单独建一套策略。

创意层只有 4 个框，**没有「配置类型」「跳转类型」「拉起方式」**：

| 字段 | 控件 | 必填 | placeholder |
|---|---|:-:|---|
| 标题文案 | 文本 | ✔ | 请输入标题文案，最多10个字 |
| 副标题文案 | 文本 | ✔ | 请输入副标题文案，最多20个字 |
| 按钮文案 | 文本 | — | 不填则默认文案 续费 |
| 跳转链接 | 文本 | — | 不填则默认购买页 |

必填判据：label 的 `::before` 是红色 `*`（`rgb(248,90,84)`）。

### PC-大会员icon与开通浮层 / PC顶导浮层（pos 52）—— 老版 v2

2026-09-04 在活动 708 建单元 41137 复核**必填范围**（之前标多了）：

| 配置形式 | 带红星的 | 选填的 |
|---|---|---|
| 文案+icon | 配置形式、标题文案、描述文案、按钮文案、跳转类型 | icon 文案、按钮角标文案、icon、跳转链接 |
| 图片形式 | 上面那些 + **Banner** | 上面那些 + **Banner跳转链接** |

⚠ 「跳转链接」页面自带默认值 `https://account.bilibili.com/big...`，留空也能存。

### 上传素材：判「传成功了没有」的坑（2026-09-04）

创意页的上传控件有两种长相：

| 页面 | 长相 | 传完之后 |
|---|---|---|
| 会员中心弹窗（图片/动画）| 一个文本框 + 「上传」按钮 | 文本框被写回 CDN 网址，下面出预览 |
| 会员中心banner、播放页催费条 | 「+ 上传」方块 | 方块里换成预览图 |

⚠ **不能拿「有没有一个含 `/bfs/` 的响应回来」当传完的判据**：`/bfs/` 既是上传
接口也是图片 CDN，**上一条创意的预览图 GET 回来同样命中**，于是提前放行、
文件还在路上就切到了下一条创意，这一条就永远空着。
实测一个单元 16 条创意，第 3 条和第 13 条的图是空的 —— 页面不标红、
点「保存创意」静默不跳转，日志里一点线索都没有。

现在的判据（`wizard_filler._send`）：
1. 只认 **POST/PUT** 的上传请求（预览图是 GET，排除掉）；
2. **页面指纹必须变**（可见图片张数 + 可见输入框的值）—— 这条是硬判据，
   请求抓没抓到都要等它变，等不到就当没传成，自动重传一次，再不行报错停下。

---

## 待抓取

剩余 61 个资源位。清单见 [资源位投放-配置项抓取.md](资源位投放-配置项抓取.md)。
